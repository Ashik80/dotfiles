---
description: Stage-aware Conventional Commit message (does not commit)
argument-hint: "[extra context]"
---
Inspect `git status --short` and `git diff --cached` (fall back to `git diff` if nothing is staged),
plus `git log --oneline -10` to match this repo's existing style.

Extra context from me: ${@:-none}

Write a Conventional Commits message:
- Subject: `type(scope): summary`, imperative mood, <= 50 chars, no trailing period.
- Body only if the "why" is not obvious from the diff. Wrap at 72 chars.
- No "generated by" trailers, no co-author lines.

Output the message in a single fenced block. Do not run `git commit` unless I explicitly say to.
